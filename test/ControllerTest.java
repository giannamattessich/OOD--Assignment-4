public class ControllerTest {
  //jsut make sure that the inputs are getting pass to the model correctly
  //within our new controller mock, have field that is an appendable log
  //all of our methods are going to log the appendable and compare them to make sure
  //that the correct methods are being passed
  
}
